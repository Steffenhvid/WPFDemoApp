<UserControl x:Class="WPFTestApplication.Views.PersonSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFTestApplication.Views"
             xmlns:viewmodels="clr-namespace:WPFTestApplication.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ComboBox Height="30"
            ItemsSource="{Binding Persons}"
            SelectedItem="{Binding CurrentPerson}"
            DisplayMemberPath="Name"/>    
    </Grid>
</UserControl>
